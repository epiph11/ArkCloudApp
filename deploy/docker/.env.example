# Copy this file to .env (gitignored) in the same folder and fill in a real value.
# docker compose automatically loads deploy/docker/.env — no extra flags needed.
#
# Generate a random 64+ character value, e.g.:
#   openssl rand -base64 48
JWT_KEY=
